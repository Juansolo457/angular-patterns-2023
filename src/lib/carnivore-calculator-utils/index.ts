export const calculateCaloriesToConsume: Function = (weight: number, activityLevel: number): void => {
  /* TODO: Implement based off inputs from the forms and return an obj to use on the table
      to render the correct values */
  return undefined;
};
